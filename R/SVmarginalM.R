#' Calculate the Smith and VanderWeele bound for the M structure, for a margianl causal estimand (Smith, L. H., & VanderWeele, T. J. (2019). Bounding bias due to selection.).
#'
#' @param a CHANGE
#' @param b CHANGE
#'
#' @return CHANGE
#' @export
#'
#' @examples CHANGE
SVmarginalM <- function(a,b)
{
  # This function calculates the SV bound for the marginal causal estimand,
  # as the weighted mean over strata.


}
